{
  "name": "via-helvetica",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "generate:localized-pages": "node scripts/generate-localized-pages.mjs",
    "predev": "npm run generate:localized-pages",
    "dev": "vite",
    "prebuild": "npm run generate:localized-pages",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate:precomputed-geneva": "node scripts/generate-precomputed-geneva-graph.mjs",
    "generate:static-geneva": "python scripts/generate-geneva-routing-cells.py"
  },
  "dependencies": {
    "ol": "^10.9.0",
    "proj4": "^2.20.9",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.9.0",
    "vite": "^8.0.0",
    "vitest": "^4.1.10"
  }
}
